# Evaluation suite scaffold

These fixtures are placeholders, not the locked Phase 1 suite and not proof of model quality. Phase 1 must replace them with licensed, versioned, fixed-seed assets and freeze their hashes before candidate training. `configured_context_tokens` and `useful_context_proven` are separate fields by design.

